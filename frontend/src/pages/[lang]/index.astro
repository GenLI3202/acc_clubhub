---
// src/pages/[lang]/index.astro
// 多语言首页
import BaseLayout from '../../layouts/BaseLayout.astro';
import { locales, t, type Locale } from '../../lib/i18n';
export function getStaticPaths() {
  return locales.map((lang) => ({ params: { lang } }));
}
const { lang } = Astro.params as { lang: Locale };
const sections = [
  {
    title: lang === 'zh' ? '慕城<br/>日常' : lang === 'de' ? 'Münchner<br/>Alltag' : 'Munich<br/>Daily',
    href: `/${lang}/events`,
    description:
      lang === 'zh'
        ? 'Social Ride、Training Day、活动报名'
        : lang === 'de'
          ? 'Social Ride, Training Day, Anmeldung'
          : 'Social Ride, Training Day, Registration',
    emoji: '🚴',
  },
  {
    title: lang === 'zh' ? '车影<br/>骑踪' : lang === 'de' ? 'Medien<br/>Archiv' : 'Media<br/>Archive',
    href: `/${lang}/media`,
    description:
      lang === 'zh'
        ? '影像资料、骑友访谈、翻山越岭记录'
        : lang === 'de'
          ? 'Videos, Interviews, Bergabenteuer'
          : 'Videos, Interviews, Mountain Adventures',
    emoji: '🎬',
  },
  {
    title: lang === 'zh' ? '器械<br/>知识' : lang === 'de' ? 'Ausrüst-<br/>ung' : 'Gear<br/>Guide',
    href: `/${lang}/knowledge/gear`,
    description:
      lang === 'zh'
        ? '购车指南、维修 Workshop、新品解读'
        : lang === 'de'
          ? 'Kaufberatung, Workshops, Neuheiten'
          : 'Buying Guide, Workshops, New Products',
    emoji: '🔧',
  },
  {
    title: lang === 'zh' ? '科学<br/>训练' : lang === 'de' ? 'Wissen-<br/>schaft' : 'Science<br/>Training',
    href: `/${lang}/knowledge/training`,
    description:
      lang === 'zh'
        ? '训练方法论、安全科普'
        : lang === 'de'
          ? 'Trainingsmethoden, Sicherheit'
          : 'Training Methods, Safety Tips',
    emoji: '📊',
  },
  {
    title: lang === 'zh' ? '骑行<br/>路线库' : lang === 'de' ? 'Routen-<br/>datenbank' : 'Route<br/>Database',
    href: `/${lang}/routes`,
    description:
      lang === 'zh'
        ? '可搜索路线数据库、Strava/Komoot 链接'
        : lang === 'de'
          ? 'Suchbare Routen, Strava/Komoot Links'
          : 'Searchable Routes, Strava/Komoot Links',
    emoji: '🗺️',
  },
];
---
<BaseLayout title={t(lang, 'nav.home')} lang={lang}>
  <section class="sections-hub">
    <div class="hub-grid">
      {sections.map((section) => (
        <a href={section.href} class="hub-card">
          {/* <span class="hub-card-emoji">{section.emoji}</span> */}
          <h3 set:html={section.title} />
          <p>{section.description}</p>
        </a>
      ))}
    </div>
  </section>
  <section class="hero">
    <img src="/images/transparent_red_logo.png" alt="ACC ClubHub" class="hero-logo" />
    <span class="status-badge status-badge--coming">
      {lang === 'zh' ? 'Beta 建设中' : lang === 'de' ? 'Beta in Entwicklung' : 'Beta Under Construction'}
    </span>
  </section>
</BaseLayout>
<style>
  .hero {
    text-align: center;
    padding: var(--space-xl) 0 var(--space-lg);
  }
  .hero-logo {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 80vh; /* Very large relative to viewport height */
    max-width: 80vw;
    height: auto;
    opacity: 0.7; /* 0.1 = Very subtle, 1 = Solid */
    z-index: 1; /* Behind content but above background */
    pointer-events: none;
    mix-blend-mode: multiply; /* Other options: screen, overlay, difference, multiply, etc.*/
  }
  .hero .status-badge {
    position: relative;
    top: 40px;
  }
  /* H1 is removed, but we keep the style block clean */
  .hero-subtitle {
    font-family: var(--font-body);
    font-size: 1.15rem;
    color: #666;
    margin-top: var(--space-sm);
    margin-bottom: var(--space-md);
  }
  .sections-hub {
    margin-top: 270px; /* Moved down by ~half card width */
  }
  .sections-hub h2 {
    margin-bottom: var(--space-sm);
  }
  .sections-hub h2::after {
    content: '';
    display: block;
    width: 40px;
    height: 3px;
    background: var(--color-accent);
  }
</style>